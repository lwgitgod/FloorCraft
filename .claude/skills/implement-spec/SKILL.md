# Implement Spec
1. Read the spec file the user provides (1 file only)
2. Read at most 3 existing source files for context
3. Start writing code immediately — no task lists, no TODO files
4. Do NOT expand scope beyond what the spec describes
5. After implementation, run any existing tests
6. Report what was implemented and what remains and save it in a same folder as the spec with [SPEC-FILENAME]-Postmortem.md naming convention
