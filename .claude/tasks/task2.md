Go through all then *B#_NAMEextractor.txt prompts text files, and then look that they have the final output format instruction, and it should be the very last thing that the prompt said. 
Just don't change anything else in the prompt, just cut out the part that starts with output format and ends with the end of the XML example and move it to the very bottom in all 22 of them. 
I.e. OUTPUT FORMAT
Your output must consist ONLY of the XML below. <outputData></outputData> at the bottom of the prompt

It might allready have it, then leave it along